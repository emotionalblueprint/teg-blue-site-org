{
  "ignoreCommand": "bash -c 'git cat-file -e ${VERCEL_GIT_PREVIOUS_SHA:-0} 2>/dev/null && git diff --quiet $VERCEL_GIT_PREVIOUS_SHA HEAD -- app/ src/ content/ public/ next.config.js package.json || exit 1'"
}
