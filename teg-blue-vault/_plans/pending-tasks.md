# Pending Tasks

Last updated: 2026-07-10.

## Homepage Gradient Science Grounding

- Homepage now introduces the Nervous System Gradient as state changes in the
  nervous system, then frames TEG-Blue as an integrative visual map of
  established research.
- Next review should check whether the hero intro feels human enough while still
  clear that TEG-Blue is organizing existing science rather than claiming a
  fully validated clinical model.
- Decide where the detailed DOI/source links in
  `_drafts/gradient-modes-science-grounding.md` should become public:
  `/scientific-foundations`, a future mode-grounding page, or a claim-audit
  bibliography.
- Review the interactive Gradient "Grounding science" readouts after more mode
  testing; current copy is mode-specific and field-level, without direct DOI
  links inside the home panel.

## Spanish Translation For teg-blue.org

- `/es/glossary` is a simplified first-reader review slice for now. It is
  intentionally not in `LIVE_PATHS` or the sitemap.
- Engine/provenance terms (`TEG-Blue Engine`, `Deep Engine`, `Visible Engine`,
  `Formation`, `Actual Conditions`) are parked from the simple glossary unless
  Anna asks for a more technical reference page.
- `state-shaped perception` / `percepción modulada por el estado` has been
  parked from the glossary for now; revisit it after the state-shaped
  perception article/foundation work is revised.
- Decide whether `Formation / Formación` should stay paired, become
  `Formation`, or become `Formación` in public Spanish copy.
- Decide whether the first public Spanish slice should keep English slugs under
  `/es` or introduce translated slugs.
- Solve the root `<html lang="en">` issue before any Spanish route is exposed
  publicly.
- `/es/foundations` is drafted for review. Next route family continues with
  core names and public map surfaces (`/`, `src/lib/gradient-data.js`,
  `src/data/deep-engine-formations.js`, and public rows from
  `src/data/deep-engine-chronic-data.js`).
- Keep Spanish routes out of `src/lib/live-paths.js` and `app/sitemap.js` until
  the first Spanish public slice is coherent and reviewed.
